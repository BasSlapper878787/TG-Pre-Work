class Surgeon {
  constructor(name, department) {
    this._name = name;
    this._department = department;
  }
}

// I learned about constructor method in JavaScript.  JavaScript calls the constructor() method every time it creates a new instance of a class.
