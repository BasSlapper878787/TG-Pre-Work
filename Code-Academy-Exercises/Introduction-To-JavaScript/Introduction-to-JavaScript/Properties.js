console.log('Teaching the world how to code'.length);


// I learned about properties in JavaScript. Every string instance has a property called length that stores the number of characters in that string.
