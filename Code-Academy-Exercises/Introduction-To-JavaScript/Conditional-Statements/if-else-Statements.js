let sale = true;

sale = false;

if(sale) {
  console.log('Time to buy!')
  } else { console.log('Time to wait for a sale.')
};

// I learned about the else statements in JavaScript, which lets us run a block of code when the "if" statement evaluates to false.
