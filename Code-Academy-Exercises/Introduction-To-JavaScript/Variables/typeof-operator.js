let newVariable = 'Playing around with typeof.';
console.log(typeof newVariable)
newVariable = 1;
console.log(typeof newVariable)

// I learned about the typeof operator in JavaScript, which allows you to check the value to its right and returns a string of the data type. This is useful when the code starts to have a lot of different data types.
