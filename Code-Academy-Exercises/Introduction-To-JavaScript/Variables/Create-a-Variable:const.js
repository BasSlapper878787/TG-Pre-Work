const entree = 'Enchiladas';
console.log(entree);
entree = 'Tacos'

// I learned about the "const" keyword in JavaScript. The "const" keyword signals that the variable cannot be reassigned, and it will render a TypeError if you try to assign it a different name.
