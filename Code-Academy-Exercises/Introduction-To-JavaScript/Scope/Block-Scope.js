function logVisibleLightWaves(){
  const lightWaves = 'Moonlight';
  console.log(lightWaves)
};
logVisibleLightWaves()
console.log(lightWaves)


// I learned about block scope in JavaScript. In block scope, variables are declared inside of blocks. These variables are called local variables because they are only available to the code that is part of the same block.
